{
  "name": "my-gitbook",
  "version": "1.0.0",
  "description": "GitBook deployment for Coolify",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npx gitbook build ./ ./_book",
    "start": "npx http-server _book -p $PORT"
  },
  "dependencies": {
    "gitbook-cli": "^2.3.2",
    "http-server": "^14.1.1"
  }
}
